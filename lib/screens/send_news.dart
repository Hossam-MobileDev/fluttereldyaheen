

import 'package:flutter/material.dart';

class SendNewsPage extends StatelessWidget {
  const SendNewsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
